apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "darwin.fullname" . }}-test-connection"
  labels:
    {{- include "darwin.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "darwin.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
